caches:
    userSearchLocales:
        configuration: "entity"
    conceptIdsByMapping:
        configuration: "entity"
    serializerWhiteListTypes:
        configuration: "entity"
    globalProperty:
        configuration: "entity"
        memory:
            maxCount: 5000
    rolePrivileges:
        configuration: "role-privileges"
