caches: 
    subscription:
        configuration: "entity"
        memory:
            maxCount: 1000
    conceptDatatype:
        configuration: "entity"
    testCache:
        configuration: "entity"
    rolePrivileges:
        configuration: "role-privileges"
