language: java
dist: trusty
jdk:
 #- openjdk8
 #- openjdk9
 #- openjdk10
 - openjdk11
 #- openjdk12
 #- openjdk13
 #- openjdk14
script: mvn clean install --batch-mode && mvn test -Pintegration-test
jobs:
    - jdk: openjdk8
branches:
  only:
    - master
after_success:
  - mvn jacoco:report coveralls:report
  - bash <(curl -s https://codecov.io/bash)
notifications:
  email: false