package org.openmrs.installer;

// public class ValidateTomcatInstall implements
// com.izforge.izpack.panels.Validator {
//
// public boolean validate(ProcessingClient client) {
// boolean result = false;
//
// if (client.getNumFields() != 3)
// return false;
//
// String url = client.getFieldContents(0);
// String username = client.getFieldContents(1);
// String password = client.getFieldContents(2);
//
// try {
// DeployTomcat.newURLCon(url, username, password);
// result = true;
// } catch (Exception e) {
// result = false;
// }
// return result;
// }
// }
